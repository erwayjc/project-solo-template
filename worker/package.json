{
  "name": "project-solo-worker",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd .. && npm install --prefer-offline",
    "start": "cd .. && npx tsx worker/index.ts"
  }
}
