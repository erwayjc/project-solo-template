{
  "name": "project-solo-worker",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd .. && npm install --cache /tmp/.npm-worker",
    "start": "cd .. && npx tsx worker/index.ts"
  }
}
